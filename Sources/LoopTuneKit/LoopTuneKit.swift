/// LoopTuneKit — core library for LoopTune.
///
/// Fetches Nightscout data, replays it through the Loop algorithm
/// (via the LoopKit `LoopAlgorithm` package), and produces tuned
/// settings recommendations (basal schedule, ISF, carb ratio).
public enum LoopTuneKit {
    /// Semantic version of the LoopTune core library.
    public static let version = "0.1.0"
}
